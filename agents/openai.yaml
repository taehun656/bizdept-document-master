interface:
  display_name: "사업부 문서 마스터"
  short_description: "사업부 결재·보고 문서를 HWPX와 Word로 생성"
  default_prompt: "Use $bizdept-document-master to create a standardized business-division report in HWPX and Word."
policy:
  allow_implicit_invocation: true
